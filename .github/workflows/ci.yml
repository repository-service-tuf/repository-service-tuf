name: CI

on:
  push:
    branches: ["main"]
    paths:
      - "tests/**"
      - "requirements.txt"
      - ".github/workflows/functional.yml"
      - ".github/workflows/ci.yml"
      - ".git/modules/**"
  pull_request:
    branches: ["main"]
    paths:
      - "tests/**"
      - "requirements.txt"
      - ".github/workflows/functional.yml"
      - ".github/workflows/ci.yml"
      - ".git/modules/**"
  workflow_dispatch:

jobs:
  functional-tests:
    uses: ./.github/workflows/functional.yml
    with:
      api_version: dev
      worker_version: dev
      cli_version: dev
